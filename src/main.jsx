import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './lista_tarefas.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <lista_tarefa />
  </React.StrictMode>,
)
